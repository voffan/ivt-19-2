<Page x:Class="AchieveNow.ProgramClasses.SeeTheList.ListOfCompetitions"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AchieveNow.ProgramClasses.SeeTheList"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Список мероприятий"
      Loaded="Page_Loaded"
      ContextMenuClosing="Page_ContextMenuClosing">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" x:Name="competitionsGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="ID" Width="30"/>
                <DataGridTextColumn Binding="{Binding Name}" Header="Название" Width="150"/>
                <DataGridTextColumn Binding="{Binding LocationId}" Header="Место проведения" Width="120"/>
                <DataGridTextColumn Binding="{Binding SportKindId}" Header="Вид спорта" Width="100"/>
                <DataGridTextColumn Binding="{Binding DateOfExecution}" Header="Дата проведения" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
